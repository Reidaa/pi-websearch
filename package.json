{
  "name": "pi-websearch",
  "version": "0.1.0",
  "description": "Web search tool for pi, backed by the Exa or Parallel hosted MCP search endpoints.",
  "keywords": [
    "exa",
    "parallel",
    "pi",
    "pi-extension",
    "pi-package",
    "websearch"
  ],
  "license": "MIT",
  "files": [
    "extensions",
    "README.md",
    "package.json"
  ],
  "type": "module",
  "scripts": {
    "format": "oxfmt .",
    "lint": "oxlint .",
    "typecheck": "tsc",
    "test": "vitest run",
    "check": "oxfmt --check . && oxlint . && tsc && vitest run"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@types/node": "^26.2.0",
    "oxfmt": "^0.63.0",
    "oxlint": "^1.78.0",
    "typebox": "*",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "engines": {
    "node": ">=22"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
